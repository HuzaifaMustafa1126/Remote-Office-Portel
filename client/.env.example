# Local development:
VITE_API_URL=http://localhost:4000/api/v1
# Production must use the public backend URL or /api/v1 when Hostinger proxies
# that path to Node. Never build production with a localhost API URL.
