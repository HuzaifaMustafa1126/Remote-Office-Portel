import {useEffect,useState} from 'react';import AttendanceFilters from '../components/attendance/AttendanceFilters';import AttendanceTable from '../components/attendance/AttendanceTable';import Loader from '../components/common/Loader';import PageHeader from '../components/common/PageHeader';import * as attendance from '../services/attendance.service';import {errorMessage} from '../utils/helpers';
const iso=d=>d.toISOString().slice(0,10);const ranges={today:()=>{const d=new Date();return{from:iso(d),to:iso(d)}},week:()=>{const d=new Date(),from=new Date(d);from.setDate(d.getDate()-((d.getDay()+6)%7));return{from:iso(from),to:iso(d)}},month:()=>{const d=new Date();return{from:iso(new Date(d.getFullYear(),d.getMonth(),1)),to:iso(d)}}};
export default function AttendanceHistoryPage(){const [filters,setFilters]=useState(ranges.month()),[rows,setRows]=useState(null),[error,setError]=useState('');useEffect(()=>{const timer=setTimeout(()=>attendance.getHistory(filters).then(setRows).catch(e=>setError(errorMessage(e))),150);return()=>clearTimeout(timer)},[filters]);return <><PageHeader title="Attendance History" description="Review your daily working time and breaks."/><div className="mb-4 flex flex-wrap gap-2">{Object.entries({today:'Today',week:'This Week',month:'This Month'}).map(([key,label])=><button key={key} onClick={()=>setFilters(ranges[key]())} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm font-semibold hover:border-indigo-400">{label}</button>)}</div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<section className="rounded-2xl border border-slate-100 bg-white shadow-sm"><AttendanceFilters filters={filters} onChange={setFilters}/>{rows?<AttendanceTable rows={rows}/>:<Loader/>}</section></>}
