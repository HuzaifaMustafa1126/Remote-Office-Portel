import {useEffect,useState} from 'react';import PageHeader from '../components/common/PageHeader';import EmptyState from '../components/common/EmptyState';import {listAuditLogs} from '../services/audit.service';import {formatDate} from '../utils/helpers';export default function AuditLogsPage(){const [logs,setLogs]=useState([]);useEffect(()=>{listAuditLogs().then(r=>setLogs(r.data))},[]);return <><PageHeader title="Audit Logs" description="A permanent record of important system activity."/><div className="overflow-x-auto rounded-2xl bg-white shadow-sm">{logs.length?<table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Action</th><th>User</th><th>Description</th><th>Date</th></tr></thead><tbody>{logs.map(l=><tr className="border-b last:border-0" key={l.id}><td className="p-4"><span className="rounded-lg bg-indigo-50 px-2 py-1 text-xs font-semibold text-indigo-700">{l.action}</span></td><td>{l.userName||'System'}</td><td>{l.description}</td><td>{formatDate(l.createdAt)}</td></tr>)}</tbody></table>:<EmptyState/>}</div></>}
