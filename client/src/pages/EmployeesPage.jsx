import {Plus,Search} from 'lucide-react';import {useEffect,useState} from 'react';import {useSearchParams} from 'react-router-dom';import Button from '../components/common/Button';import Modal from '../components/common/Modal';import PageHeader from '../components/common/PageHeader';import EmployeeForm from '../components/employees/EmployeeForm';import EmployeeTable from '../components/employees/EmployeeTable';import usePermission from '../hooks/usePermission';import * as emp from '../services/employee.service';import {listRoles} from '../services/role.service';import {errorMessage} from '../utils/helpers';import {PERMISSIONS as P} from '../utils/permissions';
export default function EmployeesPage(){const [rows,setRows]=useState([]),[roles,setRoles]=useState([]),[query,setQuery]=useState(''),[editing,setEditing]=useState(null),[open,setOpen]=useState(false),[busy,setBusy]=useState(false),[notice,setNotice]=useState(''),[params]=useSearchParams();const canCreate=usePermission(P.EMPLOYEES_CREATE),canEdit=usePermission(P.EMPLOYEES_UPDATE),canDeactivate=usePermission(P.EMPLOYEES_DEACTIVATE);async function load(){const r=await emp.listEmployees({search:query});setRows(r.data)}useEffect(()=>{load()},[query]);useEffect(()=>{listRoles().then(setRoles).catch(()=>{});if(params.get('create')&&canCreate)setOpen(true)},[]);async function save(data){setBusy(true);setNotice('');try{const r=editing?await emp.updateEmployee(editing.id,data):await emp.createEmployee(data);setNotice(r.message);setOpen(false);setEditing(null);load()}catch(e){setNotice(errorMessage(e))}finally{setBusy(false)}}async function status(e){if(!confirm(`${e.status==='ACTIVE'?'Deactivate':'Activate'} ${e.firstName}?`))return;try{await emp.setEmployeeStatus(e.id,e.status==='ACTIVE'?'INACTIVE':'ACTIVE');load()}catch(x){setNotice(errorMessage(x))}}return <><PageHeader title="Employees" description="Manage employee profiles and access." action={canCreate?<Button onClick={()=>{setEditing(null);setOpen(true)}}><span className="flex items-center gap-2"><Plus size={17}/>Add employee</span></Button>:null}/>{notice&&<div className="mb-4 rounded-xl bg-indigo-50 p-3 text-sm text-indigo-700">{notice}</div>}<div className="rounded-2xl border border-slate-100 bg-white shadow-sm"><div className="relative max-w-md p-4"><Search className="absolute left-7 top-7 text-slate-400" size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search employees…" className="w-full rounded-xl border border-slate-200 py-2.5 pl-10 pr-3 outline-none focus:border-indigo-500"/></div><EmployeeTable employees={rows} canEdit={canEdit} canDeactivate={canDeactivate} onEdit={e=>{setEditing(e);setOpen(true)}} onStatus={status}/></div><Modal open={open} title={editing?'Edit employee':'Add employee'} onClose={()=>setOpen(false)}><EmployeeForm employee={editing} roles={roles} onSubmit={save} busy={busy}/></Modal></>}
