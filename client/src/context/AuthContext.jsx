import { createContext,useCallback,useEffect,useState } from 'react'; import * as auth from '../services/auth.service';
export const AuthContext=createContext(null);
export function AuthProvider({children}){const [user,setUser]=useState(null),[loading,setLoading]=useState(true);const logout=useCallback(()=>{localStorage.removeItem('rop_token');sessionStorage.removeItem('rop_token');setUser(null)},[]);const refresh=useCallback(async()=>{const token=localStorage.getItem('rop_token')||sessionStorage.getItem('rop_token');if(!token){setLoading(false);return}try{setUser(await auth.me())}catch{logout()}finally{setLoading(false)}},[logout]);useEffect(()=>{refresh();addEventListener('auth:unauthorized',logout);return()=>removeEventListener('auth:unauthorized',logout)},[refresh,logout]);const signIn=async(credentials,remember)=>{const data=await auth.login(credentials);(remember?localStorage:sessionStorage).setItem('rop_token',data.token);const current=await auth.me();setUser(current);return current};return <AuthContext.Provider value={{user,loading,signIn,logout,refresh}}>{children}</AuthContext.Provider>}
