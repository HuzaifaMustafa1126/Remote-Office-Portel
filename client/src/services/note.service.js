import api from"./api";export const listNotes=params=>api.get("/notes",{params}).then(r=>r.data.data);export const listNoteAuthors=()=>api.get("/notes/authors").then(r=>r.data.data);export const toggleNotePin=id=>api.patch("/notes/"+id+"/pin").then(r=>r.data.data);export const getNote=id=>api.get(`/notes/${id}`).then(r=>r.data.data);export const createNote=data=>api.post("/notes",data).then(r=>r.data.data);export const updateNote=(id,data)=>api.put(`/notes/${id}`,data).then(r=>r.data.data);export const uploadImage=(id,file)=>api.post(`/notes/${id}/images`,file,{headers:{"Content-Type":file.type,"X-File-Name":encodeURIComponent(file.name)}}).then(r=>r.data.data);export const removeImage=(id,imageId)=>api.delete(`/notes/${id}/images/${imageId}`).then(r=>r.data.data);export const imageBlob=(id,imageId)=>api.get(`/notes/${id}/images/${imageId}/content`,{responseType:"blob"}).then(r=>r.data);
