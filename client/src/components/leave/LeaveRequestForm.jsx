import {useEffect,useMemo,useState} from 'react';import Button from '../common/Button';import {listDays} from '../../services/companyCalendar.service';const types=[['CASUAL','Casual Leave'],['SICK','Sick Leave'],['EMERGENCY','Emergency Leave'],['PERSONAL','Personal Leave'],['OTHER','Other']];const count=(a,b,off)=>{if(!a||!b||b<a)return 0;let n=0,d=new Date(`${a}T00:00:00Z`),end=new Date(`${b}T00:00:00Z`);while(d<=end){const date=d.toISOString().slice(0,10);if(d.getUTCDay()!==0&&!off.has(date))n++;d.setUTCDate(d.getUTCDate()+1)}return n};export default function LeaveRequestForm({onSubmit,busy}){const [form,setForm]=useState({leaveType:'CASUAL',startDate:'',endDate:'',reason:''}),[off,setOff]=useState(new Set());useEffect(()=>{if(form.startDate&&form.endDate)listDays({from:form.startDate,to:form.endDate,status:'ACTIVE'}).then(rows=>setOff(new Set(rows.filter(r=>r.dayType!=='WORKING_DAY').map(r=>r.calendarDate)))).catch(()=>{})},[form.startDate,form.endDate]);const days=useMemo(()=>count(form.startDate,form.endDate,off),[form.startDate,form.endDate,off]);const set=(k,v)=>setForm({...form,[k]:v});return <form onSubmit={e=>{e.preventDefault();onSubmit(form)}} className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Leave Type<select value={form.leaveType} onChange={e=>set('leaveType',e.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 p-2.5 font-normal">{types.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><div className="rounded-xl bg-indigo-50 p-3 text-center"><p className="text-xs text-indigo-500">Actual Working Days</p><p className="text-2xl font-black text-indigo-700">{days}</p></div><label className="text-sm font-semibold">From<input required type="date" value={form.startDate} onChange={e=>set('startDate',e.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 p-2.5 font-normal"/></label><label className="text-sm font-semibold">To<input required min={form.startDate} type="date" value={form.endDate} onChange={e=>set('endDate',e.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 p-2.5 font-normal"/></label><label className="text-sm font-semibold sm:col-span-2">Reason<textarea required minLength={5} rows={4} value={form.reason} onChange={e=>set('reason',e.target.value)} className="mt-1.5 w-full resize-none rounded-xl border border-slate-200 p-3 font-normal" placeholder="Briefly explain your leave request"/></label><div className="sm:col-span-2"><Button disabled={busy||!days}>{busy?'Submitting…':'Submit Request'}</Button></div></form>}
