import AttendanceBadge from './AttendanceBadge';import LiveWorkTimer from './LiveWorkTimer';import BreakTimer from './BreakTimer';
const time=v=>v?new Intl.DateTimeFormat('en-PK',{hour:'2-digit',minute:'2-digit'}).format(new Date(v)):'—';
export default function LiveOfficeStatus({employees=[]}){return <section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold">Live office status</h2><span className="flex items-center gap-2 text-xs font-semibold text-emerald-600"><i className="h-2 w-2 animate-pulse rounded-full bg-emerald-500"/>LIVE</span></div><div className="mt-4 max-h-[420px] space-y-2 overflow-y-auto">{employees.map(e=><div className="grid gap-3 rounded-xl border border-slate-100 p-3 sm:grid-cols-[1fr_auto_auto] sm:items-center" key={e.employeeId}><div><p className="font-semibold">{e.employeeName}</p><p className="text-xs text-slate-400">{e.employeeCode} · {e.department} · {time(e.clockInAt)}</p></div><AttendanceBadge status={e.status}/><div className="min-w-32 text-right text-sm font-semibold">{e.status==='ON_BREAK'?<BreakTimer seconds={e.currentBreakSeconds} running/>:<LiveWorkTimer seconds={e.workSeconds} running={e.status==='WORKING'}/>}</div></div>)}</div></section>}
