import {Activity} from 'lucide-react';import EmptyState from '../common/EmptyState';
export default function LiveActivityFeed({items=[]}){return <section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="font-bold">Recent attendance activity</h2>{!items.length?<EmptyState title="No attendance activity"/>:<div className="mt-3 divide-y">{items.map(x=><div className="flex gap-3 py-3" key={x.id}><div className="mt-1 grid h-7 w-7 shrink-0 place-items-center rounded-full bg-indigo-50 text-indigo-600"><Activity size={14}/></div><div><p className="text-sm">{x.description}</p><p className="mt-1 text-xs text-slate-400">{new Intl.DateTimeFormat('en-PK',{hour:'2-digit',minute:'2-digit'}).format(new Date(x.createdAt))}</p></div></div>)}</div>}</section>}
