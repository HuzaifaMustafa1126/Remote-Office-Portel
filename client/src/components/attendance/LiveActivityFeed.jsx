import {Coffee,LogIn,LogOut,Play} from 'lucide-react';import {Link} from 'react-router-dom';
const icons={ATTENDANCE_CLOCK_IN:LogIn,ATTENDANCE_CLOCK_OUT:LogOut,BREAK_STARTED:Coffee,BREAK_ENDED:Play};
export default function LiveActivityFeed({items=[]}){return <section className="h-full rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><h2 className="font-bold">Recent Activity</h2><p className="mt-0.5 text-xs text-slate-400">Latest attendance events</p></div><Link to="/audit-logs" className="text-xs font-semibold text-indigo-600 hover:text-indigo-700">View all</Link></div>{!items.length?<p className="py-10 text-center text-sm text-slate-400">No attendance activity yet today.</p>:<div className="mt-3">{items.slice(0,8).map((x,index)=>{const Icon=icons[x.action]||LogIn;return <div className={`flex gap-3 py-2.5 ${index<Math.min(items.length,8)-1?'border-b border-slate-100':''}`} key={x.id}><div className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-indigo-50 text-indigo-600"><Icon size={14}/></div><div className="min-w-0"><p className="text-sm leading-snug text-slate-700">{x.description}</p><p className="mt-0.5 text-[11px] text-slate-400">{new Intl.DateTimeFormat('en-PK',{hour:'2-digit',minute:'2-digit'}).format(new Date(x.createdAt))}</p></div></div>})}</div>}</section>}
