import{Router}from'express';import * as c from'../controllers/report.controller.js';import{requirePermission as p}from'../middleware/permission.middleware.js';import{validate}from'../middleware/validate.middleware.js';import{querySchema}from'../validators/report.validator.js';import asyncHandler from'../utils/asyncHandler.js';const r=Router();r.use(p('reports.view'),validate(querySchema,'query'));for(const[path,fn]of[['overview',c.overview],['attendance',c.attendance],['attendance/summary',c.summary],['late',c.late],['short-hours',c.short],['extra-hours',c.extra],['breaks',c.breaks],['leaves',c.leaves],['unauthorized-absence',c.absence],['configurations',c.configurations],['shifts',c.shifts],['salary',c.salaries],['payroll',c.payroll]])r.get('/'+path,asyncHandler(fn));export default r;
