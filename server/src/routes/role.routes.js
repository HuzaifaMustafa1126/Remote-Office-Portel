import {Router} from 'express';import * as c from '../controllers/role.controller.js';import {requirePermission as p} from '../middleware/permission.middleware.js';import {validate} from '../middleware/validate.middleware.js';import {roleSchema} from '../validators/role.validator.js';import asyncHandler from '../utils/asyncHandler.js';const r=Router();r.get('/',p('roles.view'),asyncHandler(c.list));r.post('/',p('roles.manage'),validate(roleSchema),asyncHandler(c.create));r.put('/:id',p('roles.manage'),validate(roleSchema),asyncHandler(c.update));export default r;
