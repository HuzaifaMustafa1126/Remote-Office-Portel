import{z}from'zod';const date=z.preprocess(v=>v===''?undefined:v,z.string().date().optional());export const querySchema=z.object({from:date,to:date,employeeId:z.preprocess(v=>v===''?undefined:v,z.coerce.number().int().positive().optional()),department:z.string().trim().max(100).optional(),status:z.string().trim().max(40).optional(),search:z.string().trim().max(100).optional(),page:z.coerce.number().int().min(1).default(1),limit:z.coerce.number().int().min(1).max(100).default(25)});
